# Error

Se elimino toda conversion `Number(orderId)` del servicio administrativo. UUID, numero local, tenant incorrecto, identificador vacio, version concurrente y usuario sin permiso cuentan con pruebas de rechazo controlado.
