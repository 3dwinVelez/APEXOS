# Prueba funcional

El script `npm run certify:auth-session` se ejecuto contra la API QA desplegada desde `develop` en el commit `60a732830809`. Verifico salud compatible, inicio de sesion con datos reales, recuperacion de identidad mediante `/api/v1/auth/me` y consulta autenticada de ordenes de servicio. Todos los pasos pasaron.
