# Prueba funcional QA

Estado: pendiente.

El navegador de validacion alcanzo la URL productiva, pero la sesion disponible no tenia permiso para Talento Humano. No se declara aprobacion funcional hasta desplegar el commit exacto en QA y comprobar que los conteos cambian en un maximo de cinco segundos sin pulsar `Abrir`.

La segunda revision detecto registros compatibles sin `route_id` directo. El resumen ahora aplica la misma asociacion por fecha operativa e identidad que el detalle antes de declarar `Sin eventos`.
