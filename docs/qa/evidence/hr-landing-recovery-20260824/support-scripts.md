# Scripts y controles

- `npm --workspace apps/web run test:hr-landing`: aprobado.
- `npm run test:promotion-scope`: aprobado.
- Typecheck, lint y build web completo: aprobados.
- `npm run certify:hr-landing:qa`: aprobado sobre el commit exacto.
- `npm run certify:platform-regression:qa`: aprobado sobre el commit exacto.
- `npm run qa:promotion:scope`: aprobado antes de publicar `develop`.
